public class hashage {
    /**
     * Exercice 3
     *
     * Class Membre {
     *  string nom;
     *  string prenom;
     *  int codePostal;
     *  string rue;
     *  string ville;
     *  int numero;
     *  Date dateNais;
     * }
     *
     * psvm(){
     * 	list<Membre> ListMbre = {...};
     *
     * }
     *
     * public string villeMaxMemnbre(List<Membre> list){
     *
     * 	string nomVille;
     * 	string maxVille;
     * 	int nbMax;
     * 	Map<chaine,entier> compteurVille : new ClasseMap<chaine, entier>();
     * 	for(int i = 0; i<list.size();i++){
     * 		int n = 0;
     * 		nomVille = list.get(i).getville();
     * 			if(compteurVille.contient(nomVille)){
     * 				compteurVille.setÉlément(nomVille,compteurs.getValeur(nomVille)+1);
     *
     * 				if(compteurVille.getValeur()>nbMax){
     * 					nbMax = compteurVille.getValeur();
     * 					maxVille = nomVille;
     *                                }* 			}
     * 			else{
     * 				compteurVille.setÉlément(nomVille,1);
     *        }* 	}
     * }
     */
}


